const answer = prompt('Каково «официальное» название JavaScript?'); // eslint-disable-line no-alert
const correctAnswer = 'ECMAScript';

if (answer === correctAnswer) {
  alert('Верно!'); // eslint-disable-line no-alert
} else {
  alert('Не знаете? «ECMAScript»!'); // eslint-disable-line no-alert
}
